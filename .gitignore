# Ignore these everywhere.
Makefile
.deps
*.o
*.a
gmon.out

# Ignore sgf files, except below regression/games. The exception is
# implemented by a rule in regression/games/.gitignore
*.sgf

# Regression test output.
regression/html/
regression/results/

# Build files in the top directory.
config.log
config.status
config.h
stamp-h
stamp-h1
autom4te.cache

# Autotools generated files (run autoreconf -i to regenerate).
configure
aclocal.m4
config.h.in
stamp-h.in
Makefile.in
compile
depcomp
install-sh
missing
mkinstalldirs
INSTALL
doc/texinfo.tex

# CMake build directory.
build/

# macOS metadata files.
.DS_Store

# Claude Code working directory.
.claude/
